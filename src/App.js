import React from "react";
import { getWeather } from "./api/funcs";

getWeather();

function App () {
  return (
    <main>
      <section>
        Clima return 
      </section>
    </main>
  )
}

export default App;
